apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: {{ .Values.namespace }}
  labels:
    app: audit-service
    {{- include "todo-app.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.auditService.replicaCount }}
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
      annotations:
        {{- if .Values.global.dapr.enabled }}
        dapr.io/enabled: "true"
        dapr.io/app-id: {{ .Values.auditService.dapr.appId | quote }}
        dapr.io/app-port: {{ .Values.auditService.dapr.appPort | quote }}
        dapr.io/log-level: "info"
        {{- end }}
    spec:
      containers:
        - name: audit-service
          image: "{{ .Values.auditService.image.repository }}:{{ .Values.auditService.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.auditService.port }}
          env:
            - name: DAPR_URL
              value: "http://localhost:3500"
            - name: STATE_STORE
              value: {{ .Values.daprComponents.statestoreName | quote }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.auditService.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.auditService.port }}
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            {{- toYaml .Values.auditService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: {{ .Values.namespace }}
  labels:
    app: audit-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.auditService.port }}
      targetPort: {{ .Values.auditService.port }}
  selector:
    app: audit-service
