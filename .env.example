# =============================================================================
# Phase III - AI Todo Chatbot Environment Variables

NEXT_PUBLIC_OPENAI_DOMAIN_KEY=your-domain-key-here

# =============================================================================
# IMPORTANT: Copy this file to .env.local and fill in actual values
# .env.local is gitignored and will NOT be committed to version control
# =============================================================================

# -----------------------------------------------------------------------------
# GitHub Configuration (for CI/CD)
# -----------------------------------------------------------------------------
GITHUB_TOKEN=
GITHUB_USERNAME=
GITHUB_REPO=

# -----------------------------------------------------------------------------
# Phase III: AI Chatbot - Cohere API Configuration
# -----------------------------------------------------------------------------
# Cohere API Key for LLM inference (intent analysis, response generation)
# Get your key from: https://dashboard.cohere.com/api-keys
COHERE_API_KEY=

# Cohere Model Selection
# Options: command-r-plus (best quality), command-r (faster, cheaper)
COHERE_MODEL=command-r-plus

# Cohere API Base URL (usually don't change)
COHERE_API_BASE_URL=https://api.cohere.ai/v1

# -----------------------------------------------------------------------------
# Phase III: Backend API Integration
# -----------------------------------------------------------------------------
# Phase II Backend API URL (deployed FastAPI service)
# This is where all task CRUD operations are routed
BACKEND_API_URL=

# -----------------------------------------------------------------------------
# Phase III: Agent Configuration
# -----------------------------------------------------------------------------
# Temperature settings for different agent types
# Lower = more deterministic, Higher = more creative
AGENT_INTENT_TEMPERATURE=0.3
AGENT_RESPONSE_TEMPERATURE=0.7

# Max tokens for agent responses
AGENT_MAX_TOKENS=1024

# -----------------------------------------------------------------------------
# Authentication (Shared with Phase II)
# -----------------------------------------------------------------------------
# MUST match the secret used in Phase II frontend/backend
BETTER_AUTH_SECRET=
OpenAi_API_Key_4_Chatbot=